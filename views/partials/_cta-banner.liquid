<div class="cta-banner">
  <div class="cta-banner-content">
    <div class="cta-banner-text">
      <strong>Ready to fix your product photos with AI?</strong>
      <span>{{ critical_count }} product{% if critical_count != 1 %}s{% endif %} need an upgrade to stop losing sales.</span>
    </div>
    <a href="#" class="btn btn--primary btn--lg">
      Fix {{ critical_count }} Product{% if critical_count != 1 %}s{% endif %} with AI Photoshoot &#8594;
    </a>
  </div>
</div>
